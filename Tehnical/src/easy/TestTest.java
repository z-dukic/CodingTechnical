package easy;

public class TestTest {

	public static void main(String[] args) {
		String pero = "pero";
		String marko = pero.substring(pero.length()-1, pero.length());
		String stevo = pero.substring(0,pero.length()-1);
		System.out.println(stevo);
		System.out.println(marko);
	}

}
